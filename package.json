{
  "name": "landing-page",
  "version": "1.0.0",
  "description": "execise with html and css",
  "main": "index.js",
  "scripts": {
    "start": "live-server --open=project",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
